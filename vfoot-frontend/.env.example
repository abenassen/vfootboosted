# mock | backend
VITE_API_PROVIDER=mock

# Used only when VITE_API_PROVIDER=backend
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Login con Google: se vuoto, il pulsante Google non viene mostrato.
VITE_GOOGLE_CLIENT_ID=
